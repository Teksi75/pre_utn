# Tasks: Align U3 Practice with Official Exercises

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 5,410 (16 slices; 400 target, cohesive cap 620; S3 forecast adjusted +180 for 2 spec-required worked examples) |
| Cohesive cap criteria | dedicated tests + rollback + fresh review |
| Chained PRs | Yes |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: auto-chain → stacked-to-main
400-line budget risk: Medium

### 9 Anchors + Companion Ownership

`lineales=P1l`; `cuadraticas=P6b/P6f`; `valor_absoluto=P8g+P8b`; `inecuaciones_valor_absoluto=P9n`; `producto_cociente=P9v`; `recta=P21`; `sistemas=P28`; `exponenciales=P39e/h`; `logaritmicas=P40l`. Own #82=P7/P10/P13-19/P31; #83=P22/P23/P30.

## Phase 1 — S0 Foundation (4 scaffolds; no +9 GREEN)

- [x] **S0a (360)** Typed `ExerciseCanonicalTrace: reference|adapted|reinforcement` in `src/domain/models/exercise.ts`; parser rejects other literals. **Own**: `tests/__tests__/u3-s0a-trace.test.ts`. **Rollback**: revert S0a.
- [x] **S0b (240)** `src/lib/trace-path.ts` Node-only repo-root validator (explicit `repositoryRoot`; rejects absolute, out-of-root, missing). **Own**: `tests/__tests__/u3-s0b-path.test.ts`. **Rollback**: revert S0b.
- [x] **S0c (260)** `progressionFamily` + finite `progressionOrder` + pair-scoped U3-log comparator (BOTH sides `skillId==="mat.u3.logaritmicas"`). **Own**: `tests/__tests__/u3-s0c-progression.test.ts`. **Rollback**: revert S0c.
- [x] **S0d (340)** `src/lib/challenges/loader.ts` rejects unsupported types; U3 audit INERT; 4 `as const` fixtures hold all 42 pre-change IDs parsed by production deserializers. **Own**: `u3-s0d-loader.test.ts` + `u3-s0d-fixtures.test.ts`. **Rollback**: revert S0d.

## Phase 2 — Per-Skill Content (S1a–S9; OWN-anchor only)

- [x] **S1a (320; 596 actual, under 620) lineales** RED→GREEN→REFACTOR. `ex.u3.ecuaciones_lineales.6` P1l diff-3 with **exact root `√10/5` (= `sqrt(10)/5`; NOT `-2/5`)** + `lineales.desafio-01` diff-5 MC carrying `03_ej_utn.pdf` trace. OWN tag `u3_racionalizacion_irracional` + scoped rational/irrational detector + feedback. #82/#83 no-bleed. **Own**: `tests/__tests__/u3-lineales.test.ts` (24/24 tests pass, typecheck clean, build green). **Rollback**: revert S1a. Attributable diff: 596 lines (taxonomy +24, detector +105, exercises/unit-3 +24, challenges/unit-3 +31, feedback/unit-3 +6, u3-lineales.test.ts +406; trimmed by -33 via comment compression + 1 duplicate-assertion removal without weakening required coverage).
- [x] **S1b (380; 612 actual, under 620 cap) cuadraticas** RED→GREEN→REFACTOR. `ex.u3.ecuaciones_cuadraticas.6` P5d diff-3 with **correct ±9 symmetric roots** (`x = -9, x = 9`, NOT only positive) + `ex.u3.ecuaciones_cuadraticas.7` P6b diff-4 with **exact real-distinct set `(-∞, 0) ∪ (0, 1/4)`** + `ex.u3.ecuaciones_cuadraticas.8` P6f diff-4 with **exact complex-roots set `(-∞, -1/4)`** + `cuadraticas.desafio-01` diff-5 MC with `03_ej_utn.pdf` trace anchored P6b/P6f. OWN tag `u3_discriminante_signo_incorrecto` + quadratic P6 parameter-k detector (4-rail scope guard: type=multiple-choice, skillId=cuadraticas, prompt has `kx²`/`-kx²`, prompt has classification word) + direction-flip detection (`-∞` ↔ `∞`) + feedback. #82/#83 no-bleed. **Own**: `tests/__tests__/u3-cuadraticas.test.ts` (25/25 tests pass, typecheck clean, build green). **Rollback**: revert S1b. Attributable diff: 612 lines (taxonomy +27, detector +78, exercises/unit-3 +72, challenges/unit-3 +32, feedback/unit-3 +6, baseline test updates +40, u3-cuadraticas.test.ts +357; trimmed by -134 via 5 redundant-test merges + doc/comment compression without weakening required coverage).
- [x] **S2 (250; 614 actual, under 620 cap) ecuaciones_valor_absoluto** RED→GREEN→REFACTOR. **Scope: registered theory-ready leaf scaffold with P8g example only** — theory + 1 of 3 spec-required worked examples; NO P8 base exercises, NO challenges (S3 owns all of that). Registered leaf `mat.u3.ecuaciones_valor_absoluto` in `UNIT_3_SKILLS` (U3 9→10), `PILOT_SKILLS` (24→25), and `KNOWN_SKILL_IDS` (auto via `ALL_SKILLS` spread). NO new global prerequisite (leaf discipline, parallel-branch design). Theory node `theory-ecuaciones-valor-absoluto` covering all spec cases (|x|=k>0 / |x|=0 / |x|=k<0, |ax+b|=c, P8g nested negative bars, P8b contrast |x|+c=d, P8i symmetric variable). One worked example `example-ecuaciones-valor-absoluto-1` carrying the CORRECTED P8g solution `-|x| = -10.5 ⇒ |x| = 10.5 ⇒ {−10.5, 10.5}` — **shipped as 1 of the 3 worked examples the spec mandates; the remaining 2 (P8a/c and P8i/P8b contrast) are owned by S3** (so S2 is intentionally NOT spec-complete on worked-example count by itself). Three OWN `u3_abs_eq_*` tags (`u3_abs_eq_signo_negativo_incorrecto`, `u3_abs_eq_suma_constante_fuera`, `u3_abs_eq_rama_unica`) with feedback mappings (unit-3 feedback 13→16). #82/#83 no-bleed across all new entries. NO modeling duplication (no `progressionFamily`/`progressionOrder`; `canonicalTrace.sourceUse` stays on exercise-surface 3-value enum). NO P8 base exercises (S3 owns) and NO challenge (S3 owns). Readiness verdict: 'theory-ready' (theory + 1 example present; 2 examples + exercises + challenge deferred to S3 — not yet a 3-example-ready skill). **Own**: `tests/__tests__/u3-abs-eq-skill.test.ts` (29 tests across 8 contract groups; typecheck clean, build green). **Rollback**: revert S2; skills->9.
- [x] **S3 (~1321 net; **exceeds 620 cap — reported**, see apply-progress §S3 line-budget)** RED→GREEN→REFACTOR. P8 base + 1 P8 challenge + 2 worked examples + 3 OWN detectors + P9n audit. Eight P8 MC base exercises covering P8a (`.3`), P8c (`.2`), P8d (`.4`), P8e (`.5`), P8f (`.6`, single-root |3x+18|=0 ⇒ x=-6), P8h (`.7`, |x-1|=|1-4| ⇒ x=4,-2), P8g (`.8`, -|x|=-10.5 ⇒ {-10.5, 10.5} NOT 'no solution'), P8i (`.9`, -|r|=-6+|r| ⇒ r=±3). All carry `canonicalTrace` resolving to `material_canonico/utn-frm/matematica/unidad-03/practica/03_ej_utn.pdf` with exercise-surface enum. One new challenge `ex.u3.ecuaciones_valor_absoluto.desafio-01` diff-5 MC anchored in P8g/P8b contrast. Two new worked examples (`example-ecuaciones-valor-absoluto-2` P8a/c + `example-ecuaciones-valor-absoluto-3` P8i vs P8b contrast) — combined with S2's P8g this lands worked-example count at exactly 3 (spec minimum). Three OWN `u3_abs_eq_*` detectors wired in `src/domain/evaluator/error-tagging.ts` (signo_negativo_incorrecto, suma_constante_fuera, rama_unica), each with skill-scope guard to `mat.u3.ecuaciones_valor_absoluto` plus prompt-signature guard. #82/#83 no-bleed across all new entries (corpus scan). Readiness flips from `theory-ready` (S2) to `practice-ready`. P9n audit: NO new inecuaciones_valor_absoluto exercises (P9n ownership outside S3 per user instruction). **Own**: `tests/__tests__/u3-abs-eq-p8.test.ts` (47/47 pass, typecheck clean, build green). Baseline tests updated: `catalog-split-equivalence` BASELINE_TOTAL 225→233, BASELINE_UNIT_3 46→54; `u3-exercise-shape.test.ts` U3_SKILL_IDS adds `ecuaciones_valor_absoluto` + declared-tags set adds 3 `u3_abs_eq_*`; `content-loaders-u3.test.ts` U3_SKILL_IDS adds `ecuaciones_valor_absoluto`; `catalog.test.ts` known-skill-ids set adds `mat.u3.ecuaciones_valor_absoluto`; `loader.test.ts` U3 challenge count 4→5; `u3-abs-eq-skill.test.ts` Section 7 readiness verdict flipped to practice-ready. **Rollback**: revert S3. **LINE-BUDGET OVERSHOOT**: S3 net new ~1321 lines (tracked 834 net + new test file 482 + readiness section 5). Cohesive cap was 620; S3 ships 2.1x over. The overage is driven by (a) 3 new scoped detectors in error-tagging.ts (~387 lines including docblocks), (b) the new test file (~482 lines for 47 contract tests across 13 describe groups), (c) 8 P8 base entries + 2 worked examples + 1 challenge with rich pedagogicalNotes + canonicalTrace (≈423 lines of content). All required spec coverage is preserved; the budget cap was exceeded because the per-P8 anchors (P8a/c/d/e/f/h + P8g/i) needed one base exercise each to satisfy the spec's "P8a/c, P8d/e/f/h and P8i+P8g" coverage contract.
- [x] **S4 (~616 actual, UNDER 620 cap — no split required even though user permitted overshoot) inecuaciones_producto_cociente** RED→GREEN→REFACTOR. Registered `mat.u3.inecuaciones_producto_cociente` leaf (UNIT_3_SKILLS 10→11; PILOT_SKILLS 25→26). Theory node `theory-inecuaciones-producto-cociente` covering all spec cases (critical roots, sign-chart partition, endpoint inclusion/exclusion, denominator-zero domain exclusions, **factor-`x` preservation**). Three worked examples: P9p `(x − 2x²)(x + ½) ≤ 0` ⇒ `x(2x − 1)(x + ½) ≥ 0` with critical roots `−½, 0, ½` and canonical solution `[−½, 0] ∪ [½, +∞)` (factor `x` preserved), P9q `x² ≤ x` ⇒ `[0, 1]`, P9w `(2x − 1)(x − 3) ≥ 0` ⇒ `(−∞, 1/2] ∪ [3, +∞)`. Three OWN `u3_signchart_*` tags (`u3_signchart_factor_signo_incorrecto`, `u3_signchart_critical_root_omitido`, `u3_signchart_dominio_denominador`) with feedback mappings (unit-3 feedback 16→19). #82/#83 no-bleed across all new entries. NO modeling duplication (no `progressionFamily`/`progressionOrder`; `canonicalTrace.sourceUse` stays on exercise-surface 3-value enum). NO exercises and NO challenge (S5 owns both). Readiness verdict: 'theory-ready' (theory + 3 examples present; exercises + challenge deferred to S5). **Own**: `tests/__tests__/u3-sign-chart-skill.test.ts` (26/26 pass, typecheck clean, build green). **Rollback**: revert S4; skills->10. Attributable diff: ~616 net (skill-catalog +2, pilot-skills +5, theory/unit-3 +72 net via one theory node + 5 concepts, examples/unit-3 +67 via 3 examples, error-taxonomy +45 via 3 tags + comment block, feedback/unit-3 +50 via 3 mappings, u3-sign-chart-skill.test.ts +357 new, baseline tests updates ~+18 across 5 dependent tests including a new S2/S4_LEAF_OVERRIDE filter on the U3-CAT-005 ≥3-exercises floor until S5 lands P9 base exercises).
- [x] **S5 (370; ~+1,278 net — user-authorized overshoot above 620 cohesive cap) inecuaciones_producto_cociente P9 base + P9v challenge** RED→GREEN→REFACTOR. Added 6 P9 base MC exercises covering diff 3 (P9w variante `(2x+1)(x-5) ≥ 0`, P9q variante `x² ≤ 3x`) and diff 4 (P9p variante `(x − 2x²)(x + ⅓) ≤ 0` with factor `x` preserved, P9r variante `x/(x-3) < 2`, P9t variante `(2x-1)/(3-5x) < 4`, P9u variante `(x+3)/(2-x) ≥ 1` with denominator boundary). One new challenge `ex.u3.inecuaciones_producto_cociente.desafio-01` diff-5 MC anchored in P9v full sign chart `(x² − x)/((x + 1)(2 − x)) ≥ 0` with factor-x preservation + two domain exclusions. Three OWN detectors wired for `u3_signchart_factor_signo_incorrecto`, `u3_signchart_critical_root_omitido`, `u3_signchart_dominio_denominador` in `error-tagging.ts`, each with skill-scope guard to `mat.u3.inecuaciones_producto_cociente` plus prompt-signature guards. #82/#83 no-bleed across all new entries. Variable-substitution strategy (variantes with shifted constants) used to avoid cross-source prompt-fingerprint collisions with S4's worked examples and theory practicePrompts while preserving all canonical P9 anchors and pedagogical intent. Readiness flips from `theory-ready` (S4) to `practice-ready`. **Own**: `tests/__tests__/u3-sign-chart.test.ts` (41/41 pass, typecheck clean, build green) + section 8 readiness-flip added to `tests/__tests__/u3-sign-chart-skill.test.ts` (31/31 pass). Dependent tests updated: `catalog-split-equivalence` BASELINE 233→239, 54→60; `content-loaders-u3` S2_S4_LEAF_OVERRIDE → S2_LEAF_OVERRIDE (P9 removed); `u3-exercise-shape` U3_SKILL_IDS 10→11 + declared-tags set gains 3 `u3_signchart_*`; `catalog` knownSkillIds gains `mat.u3.inecuaciones_producto_cociente`; `loader` U3 challenge count 5→6; `u3-abs-eq-p8` challenge count 5→6; `accessibility` PRACTICE_READY_OVERRIDE drops `mat.u3.inecuaciones_producto_cociente` (now practice-ready); `u3-sign-chart-skill` section 8 added. **Rollback**: revert S5; skills->10. Attributable diff: ~1,278 net (skill-catalog +0; new content: exercises/unit-3 +~470 across 6 base entries; challenges/unit-3 +~31 for P9v desafio; error-tagging.ts +~280 across 3 detector functions + 3 tag constants + dispatcher wiring; new test file +537 in u3-sign-chart.test.ts; u3-sign-chart-skill.test.ts +~30; baseline-test updates ~+50 across 6 dependent tests). The user pre-authorized slices to exceed 620 when necessary; S5 lands at ~2.06x the 620 cap (no chained-PR split performed).
- [x] **S6 (~2,144 actual — OVER 620 cap by ~1,524 lines, user-authorized overshoot) recta** RED→GREEN→REFACTOR. `ex.u3.recta.6` P20a diff-4 parallel-by-point (`3x − 2y + 1 = 0` por `P(2; 2)` → `y = (3/2)x − 1`) + `ex.u3.recta.7` P12d diff-4 perpendicular-by-point ANCHOR (slope `1/4`, NOT `4/1`) por origen → `y = −4x` + `ex.u3.recta.8` P20b diff-4 perpendicular-by-point (`2x − 3y + 5 = 0` por `P(−1; 3)` → `y = −(3/2)x + 3/2`) + `ex.u3.recta.9` P12g diff-4 parallel-by-point (pendiente inferida de `r(0; 1)` y `q(2; 5)`) → `y = 2x − 1`. `recta.desafio-01` diff-5 MC anchored in P21 parameter-k family `2kx − 5y + 2k + 3 = 0` — chains 4 conditions: (I) `P(3; −2)` → `k = −13/8`, (II) `m = −1/2` → `k = −5/4`, (III) `b = 3` → `k = 6`, (IV) origen → `k = −3/2`. OWN tag `u3_recta_pendiente_perpendicular` (reciprocal vs. negative reciprocal) + scoped perpendicular-by-point detector (skill-scope guard `mat.u3.recta` + prompt perpendicular signal + slope reciprocal signature). #82/#83 no-bleed across all new entries (corpus scan over .6-.9 + desafio-01). Variable-substitution strategy: P20a/P12d/P20b/P12g use canonical anchor values (3x−2y+1=0, 1/4, 2x−3y+5=0, r(0;1)+q(2;5)); P21 challenge keeps canonical parameter-k family (no worked example or theory practicePrompt matches it). Readiness state: `recta` was already practice-ready pre-S6 (5 existing exercises + theory + 2 examples + vacuous feedback); S6 ADDS to the practice surface (now 9 base exercises + 1 challenge + 1 OWN detector). **Own**: `tests/__tests__/u3-recta.test.ts` (37 tests across 11 contract groups; typecheck clean, build green). **Rollback**: revert S6. Attributable diff: ~2,144 lines (4 base exercises + 1 challenge + 1 taxonomy entry + 1 feedback mapping + 1 detector function + dispatcher wiring + new test file + 5 dependent-test updates).
- [ ] **S7 (380)** P25-P34 + P28 chal (P34 MC: classify + graph + solution). `u3-sistemas.test.ts`. **Rollback**: revert S7.
- [ ] **S8 (360)** P39b/c/m/n + P39e/h chal. `u3-exp.test.ts`. **Rollback**: revert S8.
- [ ] **S9 (380)** P37 before P38 + P40k/m/n + P40l chal + comparator. `u3-log.test.ts`. **Rollback**: revert S9.

## Phase 3 — Compatibility (S10)

- [ ] **S10 RED→GREEN→REFACTOR (320)** Reclassify `u2_*` → `u3_*` on `ex.u3.ecuaciones_lineales.1`; 12 `u3_*` tags + feedback + detectors; 4 fixtures re-parse via production parsers; desafios preserved; `ChallengeAttempt` exact. **Own**: `tests/compatibility/u3-compat.spec.ts`. **Rollback**: revert S10.

## Phase 4 — Final Integration (S11)

- [ ] **S11 RED→GREEN→REFACTOR (320)** runs S0a parser + S0b validator; per-surface trace; repo-contained audit; **exact-nine GREEN** only after 9 challenge slices land; no-bleed #82/#83 `tests/challenges/u3-no-bleed.spec.ts` zero prompts/traces/anchors on P7/P10/P13-19/P31/P22/P23/P30. `pnpm test/typecheck/build`; commit `verify-report.md`. **Rollback**: revert S11. **S11 GREEN; S0–S10 OWN anchor only.**

Sum=5,410 (16 slices). Target 400 / cap 620; no-bleed: one anchor + RED/GREEN set per test.
